{
  "$comment": "Client config for the ContextOS MCP server. Use ABSOLUTE paths. CONTEXTOS_BUNDLE points to a Context Handoff bundle exported by the engine (ContextOS.export()); omit it to run with seeded demo context.",
  "mcpServers": {
    "contextos": {
      "command": "node",
      "args": ["/abs/path/to/contextos/mcp-server/src/server.ts"],
      "env": {
        "CONTEXTOS_BUNDLE": "/abs/path/to/handoff.json"
      }
    }
  }
}

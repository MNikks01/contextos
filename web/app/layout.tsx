import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "ContextOS — your team's context, in every AI tool",
  description:
    "Capture your team's decisions, conventions, and glossary once; export a Context Handoff (CLAUDE.md / AGENTS.md) so every AI tool starts warm. Ask questions grounded in your context + code.",
  keywords: ["AI context", "CLAUDE.md", "AGENTS.md", "context handoff", "AI coding", "team memory"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
